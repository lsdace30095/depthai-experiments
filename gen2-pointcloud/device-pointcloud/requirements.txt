opencv-python
depthai==2.15.0.0
numpy
open3d==0.10.0.0; platform_machine != "armv7l" and python_version < "3.9"
#torch # Optional, only if we want to create the model locally
mistune>=2.0.3 # not directly required, pinned by Snyk to avoid a vulnerability